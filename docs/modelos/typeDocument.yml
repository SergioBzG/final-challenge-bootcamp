definitions:
  typeDocument:
    type: object
    properties:
      id:
        type: number
      name:
        type: string
      state:
        type: boolean
      created_at:
        type: string
        format: date-time
      updated_at:
        type: string
        format: date-time

components:
  schemas:
    createTypeDocument:
      type: object
      properties:
          name:
            type: string
          state:
            type: boolean
      example:
        {
          "name": "CC",
          "state": true
        }